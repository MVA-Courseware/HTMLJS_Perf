The whole project is added, except images.  HighFiveYeah.js is the
default file in the startup html and it is the slow version before
optimization.  For your reference, HighFiveYeh - Fatest.js is the optimized version and
HighFiveYeh - Slowest.js is the slow version. 
